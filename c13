#include<stdio.h>
#include<stdlib.h>
#include<time.h>
main(int x,int y)
{
    x=0;
    y=0;
    srand((unsigned)time(NULL));
    x=rand()%(100)+1;
    y=rand()%(100)+1;
    int z;
    if(x>y)z=x;
    else z=y;
    printf("a is %d,b is %d,z is %d \n",x,y,z);
    return 0;
}
